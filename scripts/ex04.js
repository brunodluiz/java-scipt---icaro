function ordenar(){

let Numeros = [];

for(let i = 1; i <= 4; i++){

    Numeros.push = (Number(prompt("informe o valor: ")));

    }
Numeros.sort((a, b)) { Numeros.sort((a, b)) => a - b};
alert("os numeros em ordem decrscemte são: +" + Numeros.join (", "));

}